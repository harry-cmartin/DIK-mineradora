quarkus.http.port=8092

quarkus.oidc.auth-server-url=http://localhost:8091/realms/quarkus
quarkus.oidc.client-id=backend-service
quarkus.oidc.credentials.secret=secret


quarkus.oidc-client.auth-server-url=${quarkus.oidc.auth-server-url}
quarkus.oidc-client.client-id=${quarkus.oidc.client-id}
quarkus.oidc-client.credentials.secret=${quarkus.oidc.credentials.secret}
quarkus.oidc-client.grant.type=password

quarkus.http.record-request-start-time=true
quarkus.log.category."io.quarkus.http.access".level=INFO

# Habilita logs detalhados do RESTEasy Reactive para ver falhas de serialização
quarkus.log.category."org.jboss.resteasy.reactive".level=DEBUG

quarkus.otel.service-name=api-gateway
quarkus.otel.sampler-type=always_on
quarkus.log.console.format=%d{HH:mm:ss} %-5p traceId=%X{traceId}, parentId=%X{parentId}, spanId=%X{spanId}, sampled=%X{sampled} [%c{2.}] (%t) %s%e%n


#Rest config

com.dikmineradora.client.ProposalRestClient/mp-rest/url=http://localhost:8081
com.dikmineradora.client.RecordRestClient/mp-rest/url=http://localhost:8082


